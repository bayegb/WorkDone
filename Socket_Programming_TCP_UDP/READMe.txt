The code is implemented in Python.
-Uses multithreading.
-Uses Json format for sending data.
-Uses sockets, both TCP and UDP.

What it does:
-The Server code uses a TCP connection to connect with many Clients.
-The Clients then query the server for the user names of other clinets.
-Then the Clients can communicate/send messages with the desired clients, using UDP protocol.
